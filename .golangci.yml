version: "2"

linters:
  default: none
  enable:
    - govet
    - misspell
  settings:
    govet:
      disable:
        # Reports every stdlib call the toolchain cannot inline. Not actionable here.
        - inline

formatters:
  enable:
    - gofmt
    - goimports
